# Copy this file to scripts/secrets.gd and fill in your token.
# secrets.gd is listed in .gitignore — never commit the real token.
#
# Get a free Mapbox token at https://account.mapbox.com/
# Restrict the token to your app's bundle ID in the Mapbox dashboard
# to prevent unauthorized use if the APK is decompiled.
const MAPBOX := "pk.YOUR_MAPBOX_TOKEN_HERE"
